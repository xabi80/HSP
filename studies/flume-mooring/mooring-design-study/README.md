# Mooring design study — technical deck

An 18-slide deck for a technical audience. It follows the leadership briefing's storyline
(`../leadership-deck/`), in technical language, and adds a slide for each configuration (single
buoy, cluster, 4×4 platform) with its plan and elevation to scale. It is built from MOORING-SPEC
rev C.1.

The slides:
- **The problem:** the summary; the test articles and their natural periods; the station-keeping
  vs non-interference conflict.
- **How it was designed:** the method; the design history.
- **The design:** the attachment-height insight (k·h²); the three configurations.
- **Results:** interference (natural-period shifts, moored vs free heave RAO) and excursion at
  H = 0.5 m.
- **Operation:** the two cord sets; the key decisions; the hardware and the daily re-check.
- **Open items:** risks, next steps and the records.

A longer, 27-slide version (installation thresholds, the k scan, the buoy's stability grid,
compliance per criterion) is in the repository history at `fd633a8`.

## Where things are

| File | What it is |
|---|---|
| Online deck (the master) | https://claude.ai/artifact/9G8QXBHsXMd7kyQyuNZbJe. It is private: share it from its **Share** menu. |
| `mooring-design-study.pdf` | A PDF of the slides, rendered locally from the files below by `render_pdf.py`. |
| `SPEAKER-NOTES.md` | The speaker notes of every slide. |
| `deck.json`, `slides/*.html` | The deck's source, copied from the online deck: the index and one file per slide (notes in each slide's `<aside>`). |
| `render_pdf.py` | Re-renders the PDF and the notes from the source (headless Edge). |
| `mooring-design-study.pptx` | *Not yet here.* Export it from the online deck (**Share › Export › PowerPoint**) and save it in this folder. |

The eight images are the project's own figures in `../figs` (generated by `mooring_figures.py`):
`plan_*`, `elevation_*`, `rationale` and `offsets`. The deck uses copies with the white margins
trimmed; `render_pdf.py` (`trim`) reproduces those copies exactly from `../figs`.

Every number comes from the study's records:
- the specification and its sources: `spec_statics*.json`, `extreme_set.json`, `attachment_sweep.json`;
- the pretension evaluation: `pretension_study.json`;
- the moored-vs-free runs: `viewer_rows_revc/`;
- the decisions: `DESIGN-BASIS.md`.

Slide 18 lists these records.

## Items the deck states that the spec does not

These come from the committed records and are itemised here, as the standing rule requires:

- **Heave natural periods** (slides 3 and 8–11) come from `attachment_sweep.json`, the chosen
  rows' `heave_T_s` and `heave_shift_pct`, also tabulated in DESIGN-BASIS F4. The free periods are derived from those two fields.
  - Free → moored: buoy 2.561 → 2.548 s, cluster 2.586 → 2.584 s, platform 2.606 → 2.606 s.
  - The extreme set: cluster 2.583 s, platform 2.605 s (`extreme_set.json`, `declare`).
- **Moored vs free heave RAO** (slide 11) comes from `viewer_rows_revc/`: 48 runs, with the free
  runs unrestrained.
  - At the tilt resonance the heave RAO changes by −0.2 to +4.6 %.
  - At T = 2.2 s, H = 0.12 m it changes by +9.0 % (buoy) and +7.8 % (cluster). That period is off
    resonance and inside the sloshing exclusion window (2.08–2.30 s).
  - Some of these values exceed criterion 6's ±3 %. The formal check is Phase D.

## Updating

1. Edit the online deck.
2. Copy its files back into this folder: `deck.json` and `slides/`.
3. Run `python render_pdf.py`.

The online deck is the master; this folder is a copy.
